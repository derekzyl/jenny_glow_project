export enum fiatTransactionStatus {
  pending = 'PENDING',
  successful = 'SUCCESSFUL',
  success = 'SUCCESS',
  failed = 'FAILED',
  error = 'ERROR',
}

export enum orderTypes {
  pending = 'PENDING',
  successful = 'SUCCESSFUL',
  failed = 'FAILED',
  success = 'SUCCESS',
  pending_ = 'pending',
  successful_ = 'successful',
  failed_ = 'failed',
  success_ = 'success',
}

