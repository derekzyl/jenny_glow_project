//
export enum ExchangeTypes {
  currency_pairs = 'CURRENCY_PAIRS',
  giftcard = 'GIFTCARDS',
}

export enum FeeTypes {
  percentage = 'PERCENTAGE',
  fixed = 'FIXED',
  fxRate = 'FX_RATE',
}

export enum InputTypes {
  manual = 'MANUAL',
  auto = 'AUTO',
}

// export const assetTypes: string[] = ['CURRENCY', 'GIFTCARD'];
